OK — đây là level “final architecture lock + implementation safety review” rồi. 3 điểm bạn raise đều đúng, và quan trọng hơn: chúng giúp tránh dev hiểu sai state machine (PRE vs POST auth) — đây là lỗi rất hay xảy ra trong multi-tenant system.

Mình chốt lại thành SPEC v1.4 FINAL (ABSOLUTE FREEZE) để bạn gửi team là implement được ngay, không còn ambiguity.

🚀 SPEC v1.4 (FINAL FREEZE — STATE MACHINE CLEARING)
🧠 1. STATE MODEL REWRITE (QUAN TRỌNG NHẤT)
❗ CHUYỂN TỪ “priority” → “state phases”

👉 Không dùng “Priority 1/2/3” nữa

🟦 PHASE 1 — PRE-AUTH RESOLUTION (CLIENT SIDE ONLY)
🎯 Mục tiêu:

Xác định “candidate center” trước khi gọi backend

✔ SOURCE ORDER (CHỈ HINT, KHÔNG FINAL):
1. URL center (deep link)
2. localStorage center
🔥 RULE:

PRE-AUTH center = “best guess”, KHÔNG phải source of truth

✔ OUTPUT:
candidateCenter
🟨 PHASE 2 — AUTH REQUEST
POST /api/mobile/auth/zalo
POST /api/mobile/auth/login
payload:
{
  accessToken?: string,
  password?: string,
  phone?: string,
  center?: candidateCenter
}
🟥 PHASE 3 — POST-AUTH SYNC (SOURCE OF TRUTH)
🎯 Đây là nơi backend quyết định FINAL STATE
✔ Backend response:
{
  token: "jwt...",
  center: "crma.easyedu.vn"
}
🔥 RULE:

backend.center ALWAYS overrides client center

🧠 2. CONFLICT RESOLUTION RULE (CRITICAL)
❗ SCENARIO bạn đưa ra là EXACT EDGE CASE
🟢 Case:
URL center = edu2
localStorage center = crma
backend center = crma
✔ FINAL RULE (LOCKED):
Source	Priority
Backend	HIGHEST (authoritative)
URL	hint only
localStorage	cache only
🔥 RESULT:
localStorage ← backend.center (overwrite ALWAYS)
❗ EXPLANATION:

User identity is bound to tenant at backend level, not deep link

🧠 3. ANSWER TO YOUR KEY QUESTION

Backend trả center khác với URL → ai thắng?

✔ FINAL ANSWER:

👉 Backend ALWAYS wins

✔ REASON:
user binding is persistent
URL is only onboarding hint
prevents tenant switching bug
ensures data consistency across devices
🧠 4. LOGIN FLOW EXTENSION (IMPORTANT FIX)
❗ Bạn đúng: password login cũng phải apply same logic
✔ FINAL RULE:
Flow	Uses center sync
Zalo login	✔
Password login	✔
OAuth future	✔
🔥 STANDARDIZED POST-AUTH STEP:
if (res.center) {
  setCenter(res.center); // ALWAYS overwrite
}
🧠 5. FINAL ARCHITECTURE (v1.4 LOCKED)
PRE-AUTH:
  URL → localStorage → candidateCenter

AUTH:
  send candidateCenter → backend

POST-AUTH:
  backend.center → overwrite localStorage

SOURCE OF TRUTH:
  backend ONLY
⚠️ 6. KEY DESIGN INSIGHT (RẤT QUAN TRỌNG)
❗ SYSTEM IS NOT:

“client decides tenant”

✔ SYSTEM IS:

“client suggests, backend decides”

🧠 7. EDGE CASE COVERAGE (NOW COMPLETE)
Case	Behavior
First open via OA	URL → backend → bind
Open without OA	backend resolves
Switch OA link	backend overrides
Reinstall app	backend restores
Multi login flows	consistent
🚀 8. FINAL SPEC SUMMARY (1 LINE)

👉 “Client only provides candidate center from URL/localStorage, backend is single source of truth for tenant, and backend center always overrides client state in post-auth sync across all login methods.”

🔥 9. FINAL VERDICT

✔ Auth system: production-ready
✔ Tenant resolution: deterministic
✔ State machine: clean (PRE / AUTH / POST)
✔ Edge cases: fully covered
✔ Multi-login support: consistent
✔ No ambiguity left for dev